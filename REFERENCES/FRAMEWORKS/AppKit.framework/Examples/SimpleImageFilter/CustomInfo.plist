{
    NSServices = (
      { NSExecutable = SimpleImageFilter;
	NSFilter = "";
        NSInputMechanism = NSUnixStdio;
        NSSendTypes = (NSTypedFilenamesPboardType:iff, NSTypedFilenamesPboardType:IFF);
        NSReturnTypes = (NSTIFFPboardType);
      }
    );
}
